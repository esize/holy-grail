- hosts: all
  connection: ssh
  become: true
  pre_tasks:
    - name: update package cache
      apt: update_cache=yes
      changed_when: False

- hosts: all
  connection: ssh
  tags: base
  become: true
  roles:
    - base
